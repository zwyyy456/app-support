# Optional build-time overrides. No API keys are needed.
# Current canonical domain; override only if deploying elsewhere.
SITE_URL=https://gitstride.hyperseek.tech
# Production is indexable by default; set false to disable indexing.
# SITE_INDEXABLE=false
